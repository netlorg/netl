before we get to a final release on netl:

 * add a format string option, e.g. "%name %ip => %ip"

 * consider an ipx logger

 * figure out why it's dumping core on sparc linux

 * store the hostname lookup cache as a binary tree
   (sorted by native version of the IP)

 * hwpassive program to passivly get hardware => IP mappings

 * port aliasing

